[Unit]
Description=Queue a Habit ePaper refresh
After=habit-epaper.service
Requires=habit-epaper.service

[Service]
Type=oneshot
ExecStart=/usr/bin/curl --fail --silent --show-error -X POST http://127.0.0.1:8000/refresh
